RB Auto Aim 自启动配置指南
本文档介绍了如何通过 systemd 调用桌面的 start.sh 脚本来实现 rb_auto_aim_debug 程序的开机自启动。这种方式方便随时在桌面上修改启动逻辑，而无需频繁改动系统底层的服务文件。

1. 创建桌面启动脚本
首先，在桌面上创建一个用于启动程序的 Shell 脚本。

打开终端，创建并编辑 start.sh 文件：

nano ~/Desktop/start.sh

将以下内容复制到文件中：

#!/bin/bash

1. 启动前等待 5 秒，确保系统环境（如相机节点、串口等）就绪
sleep 5

2. 进入程序的构建目录
cd ~/Desktop/sp_vision_25_rbclone/build

3. 执行自瞄调试程序
./rb_auto_standard_debug

保存并退出（Ctrl+O, Enter, Ctrl+X）。

赋予执行权限（必做）：

chmod +x ~/Desktop/start.sh

2. 配置 Systemd 服务文件
接下来，创建一个 systemd 服务来接管并运行这个桌面脚本。

使用 sudo 权限创建服务文件：

sudo nano /etc/systemd/system/zimiao.service

将以下内容填入文件。注意：请务必将 <你的用户名> 替换为你当前系统的真实用户名（例如 ubuntu）。

[Unit]
Description=RB Auto Aim Start Script Service

确保在网络和多用户环境加载完成后启动
After=network.target multi-user.target

[Service]
Type=simple

替换为你的实际系统用户名
User=<你的用户名>

指定工作目录为桌面
WorkingDirectory=/home/<你的用户名>/Desktop

执行桌面的启动脚本
ExecStart=/home/<你的用户名>/Desktop/start.sh

进程崩溃后自动重启
Restart=always

崩溃后等待 3 秒再重启
RestartSec=3

将输出重定向到 systemd 的日志系统
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target

保存并退出。

3. 服务管理与使用说明
配置完成后，请先重新加载 systemd 守护进程，使刚才创建的服务文件生效：

sudo systemctl daemon-reload

日常管理命令速查

操作	命令	说明
开启自启动	sudo systemctl enable zimiao.service	设置为开机自动运行
取消自启动	sudo systemctl disable zimiao.service	取消开机自动运行
立即启动	sudo systemctl start zimiao.service	手动在后台启动程序
停止运行	sudo systemctl stop zimiao.service	强制终止正在运行的程序
重启程序	sudo systemctl restart zimiao.service	重启该服务
查看状态	sudo systemctl status zimiao.service	查看是否正在运行及近期运行状态
4. 查看程序日志
由于使用了 systemd，原本在 screen 中打印的日志会被系统自带的 journalctl 工具自动收集和管理。你不再需要手动记录 log 文件。

实时查看最新日志（类似 tail -f）：

journalctl -u zimiao.service -f

查看今天的完整日志：

journalctl -u zimiao.service --since today

查看最后 100 行日志：

journalctl -u zimiao.service -n 100

