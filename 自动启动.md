# zimiao systemd 直启与 Watchdog 部署

本文将 `rb_binocular_aim_debug_async` 直接交给 systemd 管理，不再经过
`start.sh`、`bash -c` 或 `screen`。服务异常退出后 3 秒自动重启；程序主采集
循环连续 10 秒没有心跳时，systemd 判定程序卡死并强制重启。

## 1. 编译程序

Watchdog 心跳已经接入 C++ 程序。必须先重新编译，否则旧程序不会发送
`READY=1` 和 `WATCHDOG=1`，使用新 service 后会启动超时。

```bash
cd /home/rm/Desktop/sp_vision_25_rbclone
cmake -S . -B build -DOPENVINO_MAKE=ON -DTENSOR_RT_MAKE=OFF
cmake --build build --target rb_binocular_aim_debug_async -j"$(nproc)"
test -x build/rb_binocular_aim_debug_async
```

## 2. 安装服务

在目标机器人上执行：

```bash
sudo nano /etc/systemd/system/zimiao.service
```

将服务文件完整内容设置为：

```ini
[Unit]
Description=hero robomaster
After=network.target
StartLimitIntervalSec=0
StartLimitBurst=0

[Service]
Type=notify
NotifyAccess=main
User=rm
Group=rm
WorkingDirectory=/home/rm/Desktop/sp_vision_25_rbclone/build

Environment="MVCAM_COMMON_RUNENV=/opt/MVS/lib"
Environment="LD_LIBRARY_PATH=/opt/MVS/lib/64:/opt/MVS/lib/32"

ExecStart=/home/rm/Desktop/sp_vision_25_rbclone/build/rb_binocular_aim_debug_async

Restart=always
RestartSec=3

WatchdogSec=10
WatchdogSignal=SIGKILL
TimeoutStopSec=5

[Install]
WantedBy=multi-user.target
```

该配置通过 `ExecStart` 直接启动可执行文件，没有 Bash 中间层。`Type=notify`
配合程序内的 systemd 通知实现，在初始化完成后报告就绪，并由主采集循环持续
发送 Watchdog 心跳。

`src/` 下所有带 `main()` 的程序均已接入相同的 systemd Watchdog。需要运行其他
程序时，只需先编译对应目标，再将 `ExecStart` 的文件名替换为对应可执行文件；
`Type=notify`、`NotifyAccess=main` 和 `WatchdogSec` 等配置保持不变。

保存服务文件后，重新加载 systemd 并启动：

```bash
sudo systemctl daemon-reload
sudo systemctl reset-failed zimiao.service
sudo systemctl enable --now zimiao.service
sudo systemctl status zimiao.service --no-pager
```

正常状态应包含：

```text
Active: active (running)
Main PID: ... (rb_binocular_aim_debug_async)
Status: "Vision pipeline is ready"
```

确认 Main PID 对应 ELF 程序，而不是 Bash：

```bash
main_pid="$(systemctl show -p MainPID --value zimiao.service)"
ps -p "$main_pid" -o pid,ppid,comm,args
readlink -f "/proc/$main_pid/exe"
```

最后一行应为：

```text
/home/rm/Desktop/sp_vision_25_rbclone/build/rb_binocular_aim_debug_async
```

## 3. 测试异常退出自动重启

不要用 `ps | grep` 取 PID，它可能误选中 `grep` 自身。直接从 systemd 获取：

```bash
old_pid="$(systemctl show -p MainPID --value zimiao.service)"
sudo kill -9 "$old_pid"
sleep 4
new_pid="$(systemctl show -p MainPID --value zimiao.service)"
systemctl status zimiao.service --no-pager
printf 'old PID: %s, new PID: %s\n' "$old_pid" "$new_pid"
```

`new_pid` 应非 0 且不同于 `old_pid`。

## 4. 测试 Watchdog 卡死恢复

`SIGSTOP` 会冻结整个进程，使 C++ 主循环无法继续发送心跳。`WatchdogSec=10`
超时后 systemd 用 `SIGKILL` 清理进程，再等待 3 秒启动新实例：

```bash
old_pid="$(systemctl show -p MainPID --value zimiao.service)"
sudo kill -STOP "$old_pid"
sleep 14
new_pid="$(systemctl show -p MainPID --value zimiao.service)"
systemctl status zimiao.service --no-pager
printf 'old PID: %s, new PID: %s\n' "$old_pid" "$new_pid"
```

查看本次 Watchdog 事件和重启日志：

```bash
journalctl -u zimiao.service -b -n 100 --no-pager
systemctl show zimiao.service \
  -p MainPID -p NRestarts -p Result -p WatchdogUSec
```

日志应出现 `watchdog timeout`，并且 `new_pid` 与 `old_pid` 不同。

## 5. 常用运维命令

```bash
sudo systemctl restart zimiao.service
sudo systemctl stop zimiao.service
sudo systemctl start zimiao.service
sudo systemctl status zimiao.service --no-pager
journalctl -u zimiao.service -f
```

每次修改 `deploy/zimiao.service` 后，重新执行第 2 节的 `install`、
`daemon-reload` 和 `restart`。只改程序二进制时无需 `daemon-reload`，重新编译后
执行 `sudo systemctl restart zimiao.service` 即可。

## Watchdog 工作方式

- `Type=notify`：初始化完成前，服务不会被误认为已就绪。
- `WatchdogSec=10`：systemd 要求程序在 10 秒内至少发送一次心跳。
- 程序按 systemd 给出的周期一半发送心跳，本配置下约每 5 秒一次。
- 心跳位于相机读取后的主采集循环；主线程或相机读取卡死时不会继续心跳。
- `WatchdogSignal=SIGKILL`：Watchdog 超时后立即终止，包括被 `SIGSTOP` 冻结的进程。
- `Restart=always` 与 `RestartSec=3`：退出、崩溃或 Watchdog 超时后均在 3 秒后重启。
- `StartLimitIntervalSec=0` 位于 `[Unit]` 段，关闭 systemd 的启动频率限制。
